# Variables d'Environnement
# Copiez ce fichier en .env et remplissez avec vos valeurs

# Flask Secret Keys
SECRET_KEY=votre-secret-key-super-securisee-changez-moi
JWT_SECRET_KEY=autre-secret-jwt-different-changez-aussi

# Environment
ENVIRONMENT=development
# Pour production, utilisez: ENVIRONMENT=production

# Port (optionnel, auto-détecté par la plupart des plateformes)
PORT=8000

# JWT Configuration
JWT_ACCESS_TOKEN_EXPIRES=1800  # 30 minutes en secondes

# OAuth2 Configuration
OAUTH2_TOKEN_EXPIRES=1800  # 30 minutes

# Génération de secrets sécurisés:
# python3 -c "import secrets; print(secrets.token_urlsafe(32))"
